import { StyleSheet } from 'react-native';

import { Colors } from 'utils/constants/Colors';

export const stylesModalHome = StyleSheet.create({
    modalContent: {
        gap: 16,
    },
    buttonDelete: {
        backgroundColor: Colors.bittersweet,
    },
});
