import { StyleSheet } from 'react-native';

import { Colors } from 'utils/constants/Colors';

export const stylesContainerPage = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: Colors.white,
    },
});
